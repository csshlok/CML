name: UI Review Export

on:
  pull_request:
    branches: [main]

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create source archive
        run: |
          mkdir -p .artifacts
          tar --exclude=.git --exclude=node_modules --exclude=.venv --exclude=.artifacts -czf .artifacts/cml-source.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: cml-source
          path: .artifacts/cml-source.tar.gz
          retention-days: 1
