{
  "flask": [
    {
      "id": "routing",
      "question": "Where does Flask register URL rules, and which routing object stores them?",
      "required_fact_groups": [
        ["add_url_rule"],
        ["url_map"],
        ["rule"],
        ["sansio/app.py", "app.py"]
      ]
    },
    {
      "id": "contexts",
      "question": "Which objects manage Flask request and application context, and how are they activated?",
      "required_fact_groups": [
        ["requestcontext", "request context"],
        ["appcontext", "application context"],
        ["contextvar", "context var"],
        ["push", "pop"]
      ]
    },
    {
      "id": "cli",
      "question": "Which module and primary class implement Flask's command-line integration?",
      "required_fact_groups": [
        ["flask/cli.py", "flask.cli"],
        ["flaskgroup"],
        ["click"],
        ["main"]
      ]
    }
  ],
  "django": [
    {
      "id": "url_resolution",
      "question": "Which Django objects resolve an incoming URL path, and where is that resolution implemented?",
      "required_fact_groups": [
        ["urlresolver"],
        ["resolve"],
        ["django/urls/resolvers.py", "urls/resolvers.py"],
        ["urlpattern", "url_patterns"]
      ]
    },
    {
      "id": "model_save",
      "question": "How does a Django model instance move from Model.save into the lower-level database save flow?",
      "required_fact_groups": [
        ["model.save", "save"],
        ["save_base"],
        ["_save_table", "save table"],
        ["django/db/models/base.py", "db/models/base.py"]
      ]
    },
    {
      "id": "middleware",
      "question": "Where does Django build its middleware chain and which symbols are responsible?",
      "required_fact_groups": [
        ["basehandler"],
        ["load_middleware"],
        ["_middleware_chain", "middleware chain"],
        ["django/core/handlers/base.py", "core/handlers/base.py"]
      ]
    }
  ],
  "react": [
    {
      "id": "create_root",
      "question": "How does React DOM create a client root and which symbols hold and initialize it?",
      "required_fact_groups": [
        ["createroot"],
        ["reactdomroot"],
        ["createcontainer"],
        ["react-dom/src/client/reactdomroot.js", "reactdomroot.js"]
      ]
    },
    {
      "id": "hooks_dispatch",
      "question": "How does React's public useState hook reach the active hook implementation?",
      "required_fact_groups": [
        ["usestate"],
        ["resolvedispatcher"],
        ["dispatcher.usestate", "dispatcher"],
        ["react/src/reacthooks.js", "reacthooks.js"]
      ]
    },
    {
      "id": "scheduler_loop",
      "question": "Which Scheduler symbols run queued callbacks and decide whether work should continue?",
      "required_fact_groups": [
        ["workloop"],
        ["flushwork"],
        ["shouldyield", "has time remaining", "hastimeremaining"],
        ["scheduler/src/forks/scheduler.js", "scheduler.js"]
      ]
    }
  ],
  "zustand": [
    {
      "id": "public_apis",
      "question": "What are Zustand's primary public APIs for vanilla stores and React-bound stores, and where are they defined?",
      "required_fact_groups": [
        ["createstore"],
        ["src/vanilla.ts", "vanilla.ts"],
        ["usestore", "createimpl", "create"],
        ["src/react.ts", "react.ts"]
      ]
    },
    {
      "id": "persistence",
      "question": "Which Zustand middleware implements persistence, and what storage abstractions does it expose?",
      "required_fact_groups": [
        ["persist"],
        ["persiststorage"],
        ["createjsonstorage"],
        ["middleware/persist.ts", "persist.ts"]
      ]
    },
    {
      "id": "selector_subscription",
      "question": "How does subscribeWithSelector change the store subscription API?",
      "required_fact_groups": [
        ["subscribewithselector"],
        ["selector"],
        ["equality"],
        ["listener"]
      ]
    }
  ]
}
